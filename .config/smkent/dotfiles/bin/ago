#!/usr/bin/env python3

import datetime
import sys
import time

if len(sys.argv) <= 1:
    print(f"Usage: {sys.argv[0]} [:]value", file=sys.stderr)
    sys.exit(0)

v = sys.argv[1]
seconds = int(v[1:]) if v[0] == ":" else int(time.time()) - int(v)
print(datetime.timedelta(seconds=seconds))
